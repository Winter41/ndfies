.row
  .col-md-1
    = link_to "#{track.name}", album_track_path(@album, track)
  .col-md-2
    = link_to "Edit", edit_album_track_path(@album, track)
    |
    = link_to "Delete", album_track_path(@album, track), method: :delete, data: {confirm: "Are you sure?"}
